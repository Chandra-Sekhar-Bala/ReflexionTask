<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReflexionTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="TextColor">@color/black</item>
        <item name="invertTextColor">@color/white</item>
        <item name="rectangle_bg_color">@color/white</item>
        <item name="grey_border_color">@color/grey</item>
    </style>

    <style name="Theme.ReflexionTask" parent="Base.Theme.ReflexionTask" />

    <style name="Theme.MySplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_movie</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/Theme.ReflexionTask</item>
    </style>
</resources>